{
  "name": "rosettajs-i18n-preact-example",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "preact watch",
    "build": "preact build",
    "serve": "sirv build --port 8080 --cors --single"
  },
  "devDependencies": {
    "preact-cli": "^3.0.0",
    "sirv-cli": "1.0.3"
  },
  "dependencies": {
    "preact": "^10.5.13",
    "rosettajs-i18n": "^1.0.0"
  }
}
